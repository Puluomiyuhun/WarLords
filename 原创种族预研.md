# 原创种族：接入方法与工作量

2026-09-25，基于本仓库 0.8 开发版的代码和素材实测。以下是范围估算，不是报价或交付日期；1 人日按约 6–8 小时专注工作估算，美术熟练度和返工会明显改变结果。

## 结论

可以做，单人和合作可以共用同一新种族。最经济的路线是原创人物部件、沿用现有动作节奏，先做四种基础兵、云梯兵和两种特色兵，再扩展完整军队。主要成本是画风一致的动画素材与平衡，增加一个种族选项本身成本很低。

| 方案 | 内容与限制 | 工程、接入、测试 | 美术、动画 | 合计工作量 |
|---|---|---:|---:|---:|
| 换皮验证 | 复用现有军队和动作，只改配色/少量部件；只能算新阵营原型 | 1–3 人日 | 2–5 人日 | 3–8 人日 |
| 原创精简种族，建议先做 | 7 种兵：基础 4 + 云梯 + 特色 2；原创头、身体、盔甲、武器，复用动作规律；特色兵沿用已有机制 | 3–6 人日 | 7–14 人日 | 10–20 人日 |
| 完整原创种族 | 约 15 种兵、两种特色兵、法师、骑兵、攻城器械、头像/城堡；另含 1–2 个新技能 | 6–12 人日 | 14–28 人日 | 20–40 人日 |

这些估算包含一次可玩的整合和基础平衡，不含独立配音、完整剧情、重做世界地图或多轮商业品质打磨。不是说要逐张手绘几千幅画；模块化部件和重复动作可以大量复用。无法在没有确认种族体型、兵种数量和技能前给可信的人民币报价。需要预算时，可用上表分别乘工程、美术的实际人日单价。

## 当前资产规模为什么影响成本

本仓库人类 15 种兵对应 2,925 个导出动画帧、18 张图集，桌面 PNG 约 10.45 MiB；巨魔 16 种兵为 3,200 帧、25 张图集、约 16.81 MiB；恶魔 15 种兵为 2,807 帧、24 张图集、约 14.19 MiB。统计只含兵种图集，不含图标、城墙、梯子、攀爬、进城和受击效果。

这些是时间线导出的帧数量，包含重复动作和静止画面，不能直接换算成原创绘画张数。比如巨怪 94 帧、巨妖 89 帧、云梯兵 150 帧。难点在于同一人物各动作的比例、脚底落点、武器接触帧和死亡姿势能否保持一致。

手机另有半尺寸图集。加载器只按本局军队加载；添加一个种族不会让每局下载整个种族库，但同时出现更多种族会增加首局资源量，需要实机测试内存和解码时间。

## 推荐制作流程

1. 先定义身份、体型与对局定位：优势、短板、两种特色兵、可被哪些现有部队克制。暂不让它独占新资源或另一套操作方式。
2. 做一张比例表：头、躯干、手脚、武器、盾、坐骑；与已有长矛兵并排对照。优先同体型人形，避免第一版同时引入飞行和多足身体。
3. 做一名普通兵贯通管线：透明 PNG 动画序列 + 动作标签 + 逐帧攻击/移动事件 + 脚底锚点 → 图集打包 → 桌面与手机半尺寸输出 → 战场检验。新增原创素材不依赖 Flash；现有 SWF 导出工具只用于旧素材参考。
4. 用同一套部件扩展四种基础兵与云梯兵，再做两个特色兵。必须包括待机、行走、攻击、死亡；有对应功能时再补格挡、冲锋、击倒、架梯、攀城、进城等动作。
5. 接入数据：种族名称/颜色/说明、初始四兵、可购买目录、价格与数值、图标、动画图集、升级资格和 AI 特色兵池。
6. 同一份内容接入单人选族、快速野战、合作选族和商店。新种族的单人出生地需要明确配置，不能仅添加下拉选项就认为完整接入。
7. 做确定性回放、存档恢复、双方同种族/不同种族、敌军混合种族、攻城与手机加载测试，再调强度。

## 当前代码需要补的扩展点

- `content.js` 的 `races`、`units`、`atlas` 与 `order` 是共享内容入口；`balance.js` 覆盖数值；`progression.js` 控制购买和升级；`coop-conquest.cjs` 的 `SPECIALS` 控制敌军特色兵。两名玩家不需要分别开发两套兵种。
- 现在部分升级按原始 `sourceIndex` 判断民族。新种族不能随便借用旧索引，否则会继承错误的专属升级；建议将新内容的升级限制改成稳定种族键，并保留旧数据的兼容读取。
- 原 28 个地区只有旧种族。单人新种族需要一个明确的出生地/剧本配置，否则没有初始领土；合作的固定盟军起点可以直接沿用。
- 引擎已经支持现有近战、投射物、格挡、冲锋、巨人和攻城动作，但新技能仍需明确行为、伤害归属、快照字段及校验。不能只补动画或说明文字。
- 新单位要维护动作标签、伤害触发帧、贴图锚点、独立单位 ID；可以复用基础兵 ID 与规则，为新种族提供自己的皮肤图集。不要在原作 `balance.js` 中覆盖别的种族共用兵种以实现本族加成。
- 现有导出/打包脚本主要位于工作目录，尚未做成完整的仓库内原创素材构建工具。上表工程工时已为整理一个可重复执行的素材导入流程留出时间。

## 可用来验证方法的种族草案（尚未制作）

“灰岩矮人”：低速、耐打、近中程压制；被快速绕线和长射程单位克制。基础兵复用现有类型与操作。

- **重锤卫士**：高甲、慢出兵，重锤近战；初版复用锤兵攻击机制，只重做外形和数值。
- **弩炮手**：慢速直射/投射型特色兵；初版用现有投射物系统，不一次加入穿透、爆炸和击退三套机制。
- 云梯兵保留；完整版本再加入攻城器械和真正实现的施法者。

这只是低风险的制作样例，不代表已决定新民族，也没有将未实现的技能放入游戏出售。若要更鲜明的新玩法，优先给其中一种特色兵加一个可测试的新机制，再评估第二个。

## 合作征服这一版的边界

0.8 本地测试版已支持共用 28 地区地图、双方独立军备、野战/攻城和最多三族敌军；初始共有福瑞圣若一块领土，双方各 500 黄金、四种基础兵。攻下 3 个地区后最多两族，6 个后最多三族；敌军种族来自目标地区及邻区的不同种族。每波合计八个单位，特色波隔路安排特色兵，间隔从 9 秒逐渐降至 6 秒，持续到胜败。

联盟种族没有随每波切换：每个 AI 种族有独立部队归属，统一站在右方。巨人、投掷兵等使用本族画面。守军未加入尚未实现的高阶法术；现有法术限制与 0.7 相同。

地图、胜败、购物和准备状态均由服务器确认。更换目标取消双方准备；胜利双方分别领奖并回商店，失败保留军备重试。活跃战斗不再因为房间创建满一小时被强制结束；整备闲置一小时和断线 30 秒清理仍保留。

目前合作进度仍只在服务器内存中，服务器重启/房间结束不会保留长期战役。模型的快照恢复已测试，但尚无玩家可用的合作存档入口或服务器持久化。这应是长战役公开推广前的下一项工作；数值也仍需双人实战校准。公网尚未发布这一版。
